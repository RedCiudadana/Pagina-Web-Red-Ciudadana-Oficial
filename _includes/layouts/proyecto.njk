{% extends "base.njk" %}

{% block head %}
<title>{{ title }}</title>
{% endblock %}

{% block content %}

<!-- Banner-area-start -->
<div class="breadcrumb-area pt-150 pb-155" style="background-image:url(/assets/img/red/05.png)">
    <div class="container">
        <div class="row">
            <div class="col-xl-12">
                <div class="breadcrumb-text text-center">
                    <h1 style="font-size:45px">{{ title }}</h1>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Banner-area-end -->

<!--POST-->
<div class="container " >

  <div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12">
      <article>
        
        <section class="content text-justify">
          <div class="row mt-40 mb-40">
            
            <div class="col-xl-8 col-lg-8 col-md-8 col-sm-12 px-4 h2-margin">
              <h6 class=" font-bold text-1xl text-gray-800">
                {{ description }}
              </h6>

              <div class=" text-sm text-gray-600 mb-40">
                Autor:
                  <span class="text-sm font-semibold text-gray-900 ml-2">
                    <!--autor correo fecha-->
                        {{ autor }} | {{ correo }} | {{ date|dateFormat('DD/MM/YYYY')}}
                  </span>
              </div>
              <div class="content-blog mb-4">
                {{ descripcion | safe}}
                
              </div>
              <a class="btn" target="_blank" href="{{enlace}}">Visita nuestra página</a>
            </div>
            <div class="col-xl-4 col-lg-4 col-md-4 col-sm-12 px-4" >
              <h6>Escucha nuestros podcasts</h6>
              <div class="w-full">
                {% if collections.podcasts %}
                  {% for podcast in collections.podcasts | reverse | limit(4) %}
                    <div class="block mb-4">
                      <iframe src="{{ podcast.data.link }}" width="100%"
                        height="152" frameBorder="0" allowfullscreen="" class="rifra"
                        allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"></iframe>
                    </div>
                  {% endfor %}
                {% endif %}
                {% if collections.addsHighlighted %}
                  {% for anuncio in collections.addsHighlighted | limit(1) %}
                    <div class="block mb-4">
                      <a href="{{anuncio.data.enlacered}}"><embed width="100%" src="{{anuncio.data.enlacev}}"></a>
                    </div>

                    <div class="block mb-4">
                      <a href="{{anuncio.data.enlacered2}}"><embed width="100%" src="{{anuncio.data.enlacev2}}"></a>
                    </div>
                  {% endfor %}
                {% endif %}
              </div>
            </div>  
          </div>

        </section>
        
      </article>
    </div>

    <!-- event-area-start -->
    <div class="event-area pt-80 pb-130">
        <div class="container">
            <div class="breadcrumb-text text-left">
                <h2 class="font-bold text-xl border-b-2 border-yellow-500 mb-4 pb-4">Más Proyectos</h2>
            </div>

            <div class="row">
            {% set sortedProyectos = collections.proyectos | sortById %}

            {% for proyecto in sortedProyectos | reverse | limit(5) %}
            {% if page.url !== proyecto.url %}

              <!-- Blog -->
              <div class="col-xl-4 col-lg-4 col-md-6">
                  <div class="case-wrapper case-02-wrapper mb-30">
                      <div class="case-img">
                          <a target="_blank" href="{{proyecto.url}}"><img src="{{proyecto.data.foto}}" alt="{{proyecto.data.title}}"></a>
                      </div>
                      <div class="case-text">
                          <h3><a target="_blank" href="{{proyecto.data.enlace}}"> {{proyecto.data.title}} </a></h3>
                          <span>{{proyecto.data.date | dateFormat('DD / MM / YYYY')}}</span>
                      </div>
                      <div class="case-info custom-overflow-hidden" style="height:120px">
                          <p class="text-overflow2"> {{proyecto.data.descripcion}} </p>
                      </div>
                      <div class="b-button">
                          <a target="_blank" href="{{proyecto.url}}"><span>Mas Información</span> <i class="far fa-long-arrow-right"></i></a>
                      </div>
                  </div>
              </div>

            {% endif %}
            {% endfor %}
            </div>

        </div>
    <div>

  </div>
</div>

{% endblock %}